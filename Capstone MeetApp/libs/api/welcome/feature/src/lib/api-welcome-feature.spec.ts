import { apiWelcomeFeature } from './api-welcome-feature';

describe('apiWelcomeFeature', () => {
  it('should work', () => {
    expect(apiWelcomeFeature()).toEqual('api-welcome-feature');
  });
});
