import { apiHomeDataAccess } from './api-home-data-access';

describe('apiHomeDataAccess', () => {
  it('should work', () => {
    expect(apiHomeDataAccess()).toEqual('api-home-data-access');
  });
});
