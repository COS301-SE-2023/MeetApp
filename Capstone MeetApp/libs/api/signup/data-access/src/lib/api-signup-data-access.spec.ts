import { apiSignupDataAccess } from './api-signup-data-access';

describe('apiSignupDataAccess', () => {
  it('should work', () => {
    expect(apiSignupDataAccess()).toEqual('api-signup-data-access');
  });
});
