import { appSignupDataAccess } from './app-signup-data-access';

describe('appSignupDataAccess', () => {
  it('should work', () => {
    expect(appSignupDataAccess()).toEqual('app-signup-data-access');
  });
});
