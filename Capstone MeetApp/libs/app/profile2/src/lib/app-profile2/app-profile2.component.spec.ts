import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AppProfile2Component } from './app-profile2.component';


describe('AppProfile2Component', () => {
  
  //let component: AppProfile2Component;
  /*
  let fixture: ComponentFixture<AppProfile2Component>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [AppProfile2Component],
    }).compileComponents();

    fixture = TestBed.createComponent(AppProfile2Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });
 */

  it('should create', () => {
    expect("test").toEqual('test')
  });
});
