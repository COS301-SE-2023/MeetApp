import { appWelcomeDataAccess } from './app-welcome-data-access';

describe('appWelcomeDataAccess', () => {
  it('should work', () => {
    expect(appWelcomeDataAccess()).toEqual('app-welcome-data-access');
  });
});
