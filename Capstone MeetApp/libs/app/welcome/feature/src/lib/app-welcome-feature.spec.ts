import { appWelcomeFeature } from './app-welcome-feature';

describe('appWelcomeFeature', () => {
  it('should work', () => {
    expect(appWelcomeFeature()).toEqual('app-welcome-feature');
  });
});
