import { appProfileDataAccess } from './app-profile-data-access';

describe('appProfileDataAccess', () => {
  it('should work', () => {
    expect(appProfileDataAccess()).toEqual('app-profile-data-access');
  });
});
