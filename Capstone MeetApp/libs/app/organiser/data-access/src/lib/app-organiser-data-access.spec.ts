import { appOrganiserDataAccess } from './app-organiser-data-access';

describe('appOrganiserDataAccess', () => {
  it('should work', () => {
    expect(appOrganiserDataAccess()).toEqual('app-organiser-data-access');
  });
});
