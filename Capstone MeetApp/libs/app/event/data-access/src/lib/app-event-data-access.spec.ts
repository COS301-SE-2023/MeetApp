import { appEventDataAccess } from './app-event-data-access';

describe('appEventDataAccess', () => {
  it('should work', () => {
    expect(appEventDataAccess()).toEqual('app-event-data-access');
  });
});
