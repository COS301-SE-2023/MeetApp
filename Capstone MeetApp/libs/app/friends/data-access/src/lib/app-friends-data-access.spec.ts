import { appFriendsDataAccess } from './app-friends-data-access';

describe('appFriendsDataAccess', () => {
  it('should work', () => {
    expect(appFriendsDataAccess()).toEqual('app-friends-data-access');
  });
});
